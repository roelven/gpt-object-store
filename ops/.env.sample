# GPT Object Store Environment Configuration
# Copy this file to .env and update with your values

# Required Configuration
# ======================

# Public URL where your API is accessible (required for OpenAPI spec)
API_URL=https://api.yourdomain.com

# Database Configuration
# ======================

# PostgreSQL credentials (change these in production!)
POSTGRES_USER=gptstore
POSTGRES_PASSWORD=change-me-to-a-secure-password
POSTGRES_DB=gptstore

# API Configuration
# =================

# Rate limiting (format: type:count/period)
RATE_LIMITS=key:60/m,write:10/m,ip:600/5m

# CORS origins (comma-separated, * for all)  
CORS_ORIGINS=*

# Logging level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
LOG_LEVEL=INFO

# API host and port configuration
API_HOST=0.0.0.0
API_PORT=8000

# External port mapping for Docker (change if port 8000 is in use)
# This maps external port to internal port 8000
# Example: EXTERNAL_API_PORT=9000 makes API available on localhost:9000
EXTERNAL_API_PORT=8000

# Pagination defaults
DEFAULT_PAGE_SIZE=50
MAX_PAGE_SIZE=200

# Backup Configuration
# ====================

# Number of days to retain backups
BACKUP_RETENTION_DAYS=14

# For testing: run backup every N minutes instead of daily at 02:30
# Uncomment to enable test mode
# BACKUP_INTERVAL_MINUTES=5